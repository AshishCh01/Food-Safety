# This project keeps environment configuration per-service.
# Copy the service-level templates before running locally or with Docker:
#
#   Backend/.env.example  -> Backend/.env
#   Frontend/.env.example -> Frontend/.env
